version: 1.2.1

interface:
  modelSelect: true
  agents: true
  runCode: false
  webSearch: false

# Define which endpoints should appear in the dropdown
modelSpecs:
  enforce: false
  # This creates a whitelist - only "custom" will show up
  addedEndpoints: 
    - "More Models"

  # You must have at least one model spec defined
  list:
    - name: "Default"
      label: "Default"
      default: true
      preset:
        endpoint: "More Models"


endpoints: 
  custom:
    - name: "More Models"
      apiKey: "${LITELLM_MASTER_KEY}"
      baseURL: "http://litellm:4000/v1"

      models:
        # Must match the model names LiteLLM returns from /v1/models
        default: ["gpt-5-mini"]
        fetch: true

      titleConvo: true
      titleModel: "gpt-5-mini"
      summarize: false
      summaryModel: "gpt-5-mini"
      forcePrompt: false


ocr:
  mistralModel: "mistral-ocr-latest"
  apiKey: "${MISTRAL_API_KEY}"
  strategy: "mistral_ocr"